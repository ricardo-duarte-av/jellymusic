package pt.aguiarvieira.jellymusic.ui.theme

import androidx.compose.material3.ExperimentalMaterial3ExpressiveApi
import androidx.compose.material3.Typography

/**
 * Expressive typography. We start from the M3 Expressive defaults (which already provide the
 * *Emphasized styles used across the app) and only override when we introduce a custom font.
 */
@OptIn(ExperimentalMaterial3ExpressiveApi::class)
val JellyMusicTypography: Typography = Typography()
