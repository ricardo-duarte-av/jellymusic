# Android / Gradle
*.iml
.gradle/
/local.properties
.idea/
.DS_Store
/build
build/
/captures
.externalNativeBuild
.cxx
*.apk
*.aab
*.ap_
*.dex
/app/release

# Compiled class file
*.class

# Log file
*.log

# BlueJ files
*.ctxt

# Mobile Tools for Java (J2ME)
.mtj.tmp/

# Package Files #
*.jar
*.war
*.nar
*.ear
*.zip
*.tar.gz
*.rar

# Keep the Gradle wrapper jar despite the *.jar rule above
!gradle/wrapper/gradle-wrapper.jar

# Signing material — never commit keystores or credentials
/signing/
*.jks
*.keystore

# CI-generated screenshots: regenerated by the Screenshots workflow and kept only as a
# build artifact (see .github/workflows/screenshots.yml). Not committed — it bloated history
# by ~6MB per run. The curated screenshots/*.jpg used in the README are kept, just not this dir.
/screenshots/generated/

# virtual machine crash logs
hs_err_pid*
replay_pid*

# Kotlin
.kotlin/

#local buildscripts
debug-build.sh

# Project instructions (holds test-server credentials — keep local only)
CLAUDE.md
